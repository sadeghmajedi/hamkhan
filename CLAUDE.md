# CLAUDE.md

راهنمای این ریپو برای Claude Code و هر کسی که روی این پروژه کار می‌کند.

## معرفی سریع

**هم‌خوان** یک PWA تک‌فایلی برای تمرین روخوانی و دیکته‌ی فارسی (مخصوص کودکان) است. بدون بک‌اند اختصاصی؛ فقط یک Cloudflare Worker واسط برای تشخیص گفتار (Azure Speech) دارد. داده‌های کاربر فقط و فقط در `localStorage` همان مرورگر/دستگاه ذخیره می‌شود.

## ۱. معماری اپ

### ساختار فایل‌ها
```
index.html      کل اپ: HTML + CSS + JS در یک فایل (بدون build step، بدون framework)
sw.js           Service Worker — کش‌کردن پوسته‌ی اپ برای اجرای آفلاین
manifest.json   متادیتای PWA برای Add to Home Screen (نام، آیکون، رنگ، display: standalone)
version.json    فایل مستقل برای قابلیت «بررسی نسخه» در تنظیمات
icon-192.png / icon-180.png / icon-512.png   آیکون‌های PWA
README.md       راهنمای نصب/اجرا و محدودیت‌های شناخته‌شده
```
هیچ‌کدام از این فایل‌ها bundle یا minify نمی‌شوند؛ همان‌طور که هستند سرو می‌شوند (روی GitHub Pages).

بخش‌های اصلی `index.html` (به ترتیب کامنت‌های داخل فایل):
- **DB** — بارگذاری/ذخیره‌ی state در localStorage (`loadDB`, `saveDB`, `defaultDB`)
- **Navigation** — سوییچ بین صفحات اپ (`go`, `currentScreenName`)
- **Persian text normalization & diff** — نرمال‌سازی متن فارسی و الگوریتم مقایسه‌ی کلمات (`normalizePersianText`, `levenshtein`, `alignWords`, `gradeAgainstReference`)
- **Speech Recognition** — ضبط صدا در مرورگر و ارسال به یک Cloudflare Worker (`WORKER_BASE_URL`) که خودش از Azure Speech برای تبدیل گفتار به متن استفاده می‌کند؛ این مسیر روی iOS هم کار می‌کند (برخلاف `webkitSpeechRecognition`)
- **Reading flow / Library / PDF import / Dictation / Stats / Settings** — منطق هر بخش از UI

### کلید localStorage
همه‌ی داده‌های کاربر (پروفایل‌ها، تاریخچه‌ی جلسات، متن‌های دلخواه) زیر یک کلید ذخیره می‌شوند:
```js
const DB_KEY = "hamkhan_db_v1";
localStorage.setItem(DB_KEY, JSON.stringify(db));
```
اگر ساختار `db` به‌شکلی تغییر کرد که با داده‌های قدیمی سازگار نیست، باید نسخه‌ی این کلید عوض شود (مثلاً `hamkhan_db_v2`) — وگرنه داده‌ی کاربران قدیمی هنگام parse خراب می‌شود. تا امروز چنین migration ای انجام نشده.

### استراتژی کش (`sw.js`)
- `install`: تمام فایل‌های `APP_SHELL` (`./`, `index.html`, `manifest.json`, آیکون‌ها) را در یک کش به نام `CACHE_NAME` (`hamkhan-cache-v<version>`) از پیش ذخیره می‌کند و بلافاصله `skipWaiting()` صدا می‌زند.
- `activate`: هر کش قدیمی که اسمش با `CACHE_NAME` فعلی فرق دارد را پاک می‌کند و `clients.claim()` صدا می‌زند تا SW جدید فوراً کنترل صفحات باز را هم به‌دست بگیرد.
- `fetch`: فقط درخواست‌های GET هم‌مبدأ را دست می‌زند؛ استراتژی **cache-first با fallback به network** است (و برعکس، اگر از کش پیدا نشد یا نتی مشکل داشت). بقیه‌ی درخواست‌ها (فونت‌ها، `version.json`، تماس با Worker تشخیص گفتار) دست‌نخورده به شبکه می‌روند.

چون نام کش شامل شماره‌ی نسخه است، هر بار که `CACHE_NAME` در `sw.js` عوض شود، فایل `sw.js` خودش هم یک بایت فرق می‌کند و مرورگر متوجه‌ی نسخه‌ی جدید SW می‌شود — به همین دلیل نگه‌داشتن نسخه در `CACHE_NAME` حیاتی است، نه فقط تزئینی.

## ۲. جاهایی که شماره‌ی نسخه وجود دارد

شماره‌ی نسخه باید همزمان در **سه جا** آپدیت شود، وگرنه یا کش کهنه می‌ماند یا UI نسخه‌ی غلط نشان می‌دهد:

| فایل | متغیر/کلید | نقش |
|---|---|---|
| `index.html` | `const APP_VERSION = "x.y.z";` (نزدیک بالای بلاک اسکریپت اصلی) | مبنای نمایش نسخه‌ی فعلی در تنظیمات و مقایسه در `checkVersion()` |
| `sw.js` | `const CACHE_NAME = 'hamkhan-cache-vx.y.z';` | باعث می‌شود نصب/فعال‌سازی SW کش قبلی را invalidate کند |
| `version.json` | `"version": "x.y.z"` | فایلی که از شبکه fetch می‌شود تا اپ نسخه‌ی نصب‌شده را با آخرین نسخه‌ی منتشرشده مقایسه کند (`checkVersion()` در تنظیمات) |

هر سه باید **دقیقاً یک عدد یکسان** داشته باشند. اگر فقط `version.json` آپدیت شود ولی `CACHE_NAME` در `sw.js` عوض نشود، Service Worker متوجه‌ی نسخه‌ی جدید نمی‌شود و فایل‌های کش‌شده (از جمله خود `index.html` قدیمی) همچنان سرو می‌شوند، هرچند `version.json` بگوید نسخه‌ی جدیدتری هست.

## ۳. قانون: `updateViaCache: 'none'` روی iOS الزامی است

ثبت Service Worker همیشه باید این‌طور باشد:
```js
navigator.serviceWorker.register('sw.js', {updateViaCache: 'none'});
```
بدون این گزینه، خود فایل `sw.js` می‌تواند توسط کش HTTP معمولی مرورگر (نه Cache Storage) کش شود. در این حالت، حتی وقتی `CACHE_NAME` داخل `sw.js` را عوض می‌کنی، مرورگر (به‌خصوص Safari/WebKit روی iOS) ممکن است همان نسخه‌ی کهنه‌ی `sw.js` را از کش HTTP بدهد و هیچ‌وقت نسخه‌ی جدید دانلود نشود — یعنی کاربر برای همیشه روی نسخه‌ی قدیمی گیر می‌کند، بدون هیچ خطا یا نشانه‌ای.

این تنظیم فعلاً در `index.html` (نزدیک انتهای فایل، در بلاک ثبت SW) رعایت شده؛ در هر تغییر بعدی روی منطق ثبت SW، این گزینه نباید حذف شود.

کنار این تنظیم، یک listener روی `controllerchange` هم وجود دارد که با فعال‌شدن نسخه‌ی جدید SW، صفحه را reload می‌کند تا کاربر بدون نیاز به بستن دستی تب/اپ، نسخه‌ی جدید را ببیند.

## ۴. محدودیت مهم: PWA نصب‌شده در iOS و Safari دو فضای کاملاً جدا هستند

روی iOS/iPadOS، وقتی کاربر اپ را با «Add to Home Screen» نصب می‌کند، آیکون روی Home Screen یک **کانتینر مستقل** از تب‌های Safari است:
- **Service Worker** جداگانه ثبت و نگه‌داری می‌شود.
- **localStorage** (و به‌تبع آن `hamkhan_db_v1` با تمام تاریخچه/پروفایل‌های کاربر) جداگانه و **مشترک نیست**.

یعنی داده‌ای که کاربر داخل اپ نصب‌شده روی Home Screen ذخیره کرده، در Safari معمولی دیده نمی‌شود و برعکس.

**نتیجه‌گیری حیاتی:** پاک‌کردن اپ از روی Home Screen (Remove/Delete) یعنی از دست رفتن کامل و بی‌بازگشت داده‌های آن پروفایل (چون جایی جز `localStorage` همان کانتینر ذخیره نمی‌شود). به همین دلیل:
- **هرگز** «پاک کن و دوباره نصب کن» یا «اپ را حذف کن و از هوم اسکرین دوباره اضافه کن» را به‌عنوان راه‌حل رفع مشکل (باگ کش، نسخه‌ی قدیمی، خطای عجیب) به کاربر پیشنهاد نده.
- راه‌حل درست برای «نسخه گیر کرده»، اصلاح ثبت SW (قانون بخش ۳) و/یا راهنمایی کاربر برای reload دستی/بستن-باز‌کردن تب است، نه حذف اپ.
- قبل از هر تغییری که ریسک از‌دست‌رفتن داده دارد، به کاربر یادآوری کن از دکمه‌ی «خروجی گرفتن» در تنظیمات (export backup) استفاده کند.

## ۵. قوانین کار روی این ریپو

- **ویرایش هدفمند، نه بازنویسی کامل.** `index.html` تک‌فایلی و نسبتاً بزرگ است؛ تغییرات باید با `Edit`/diff مینیمال روی همان تابع یا بلاک مربوطه انجام شود، نه rewrite کل فایل یا کل تابع بدون نیاز.
- **قبل از commit، جاوااسکریپت را validate کن.** چون هیچ build/lint pipeline‌ای در این ریپو نیست، سینتکس اشتباه فقط موقع اجرا در مرورگر کشف می‌شود. حداقل با `node --check` روی بلاک‌های JS (یا استخراج و parse‌کردن اسکریپت داخل `index.html`) قبل از commit مطمئن شو که syntax error ندارد.
- **تست روی دستگاه واقعی، نه فقط دسکتاپ.** بخش تشخیص گفتار و رفتار Service Worker/PWA (به‌خصوص `updateViaCache`, `controllerchange`, و جداسازی localStorage) روی iOS/iPadOS واقعی رفتار متفاوتی از دسکتاپ کروم دارند. تست دسکتاپ برای منطق UI کافی است ولی برای تغییرات مربوط به SW، کش، یا میکروفون/تشخیص گفتار کافی نیست — قبل از این‌که چنین تغییری را «تمام‌شده» اعلام کنی، صریحاً بگو که فقط روی دسکتاپ تست شده و تست روی دستگاه واقعی هنوز لازم است.
- هر تغییری که نسخه را بالا می‌برد، باید هر سه محل بخش ۲ را همزمان آپدیت کند.

## ۶. بررسی: برچسب نسخه‌ی hardcode‌شده در UI

طبق بررسی فعلی کد، در این ریپو (`hamkhan`) چنین باگی **پیدا نشد**: تنها جایی که شماره‌ی نسخه در UI دیده می‌شود المان `<span id="currentVersion">` است که مقدارش همیشه در زمان اجرا از طریق `renderSettings()` با `document.getElementById('currentVersion').textContent = APP_VERSION` پر می‌شود؛ هیچ رشته‌ی نسخه‌ای به‌صورت متن ثابت در HTML یا در `<title>` نوشته نشده است.

اگر در آینده نسخه‌ای از این کد اضافه شد که شماره‌ی نسخه را مستقیم در markup (نه از طریق `APP_VERSION`) می‌نویسد — دقیقاً همان الگوی باگی که قبلاً در پروژه‌ی PFM دیده شده — باید فوراً به یک منبع واحد (`APP_VERSION`) وصل شود.
